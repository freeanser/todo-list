{{!-- todos --}}
<a href="./todos/new">Create</a>
<ul>
  {{#each todos}}
  <li>
    {{{this.name}}}
    <a href="./todos/{{ this._id}}">detail</a> {{!-- 找到資料庫中的_id --}}
    <a href="./todos/{{ this._id}}/edit">edit</a>
    <form action="./todos/{{this._id}}/delete" method="POST" style="display: inline;">
      <button type="submit">delete</button>
    </form>
  </li>
  {{/each}}
</ul>